<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/mid_green</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/login_form_details</item>
        <item name="colorControlActivated">@color/login_form_details_medium</item>
        <item name="colorControlHighlight">@color/login_form_details</item>
    </style>

</resources>
